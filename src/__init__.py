"""Source-to-target mapping pipeline for vehicle service and repair records.

The sub-packages mirror the stages of the pipeline described in README.md.
Each one is filled in during the phase named in its own docstring, so an empty
package means "not built yet", not "lost".
"""

__version__ = "0.1.0"
